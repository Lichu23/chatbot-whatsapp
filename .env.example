# Meta WhatsApp Cloud API
META_WHATSAPP_TOKEN=your_permanent_access_token
META_PHONE_NUMBER_ID=your_phone_number_id
META_VERIFY_TOKEN=my_verify_token_123
META_APP_SECRET=your_meta_app_secret

# Supabase
SUPABASE_URL=https://test123.supabase.co
SUPABASE_KEY=

# ── Cascading LLM Providers (tries in order: Groq → Cerebras → Mistral → OpenRouter) ──
# On 429 rate limit, instantly falls to the next provider. All free tiers.
# You need at least ONE key. More keys = more resilience.

# Groq (primary — blazing fast, 30 req/min free)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# Cerebras (fallback #1 — even faster, 30 req/min free)
CEREBRAS_API_KEY=
CEREBRAS_MODEL=llama3.1-8b

# Mistral (fallback #2 — 1B tokens/month free)
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest

# OpenRouter (fallback #3 — aggregates free models)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.3-70b-instruct:free

# Meta Catalog
CATALOG_ID=

# Server
PORT=3000

# Timezone (default: America/Argentina/Buenos_Aires)
TIMEZONE=Europe/Madrid

# Error alerting — receive WhatsApp messages on critical errors (optional)
ALERT_PHONE=5491100000000

